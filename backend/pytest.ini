[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
addopts = -ra --strict-markers
markers =
    integration: tests nécessitant MySQL (défaut en CI)
